摘要（报告已写入 `D:\CCNI\expHarness\accept-reverify.report.md`）：

1. 1.1–1.8 全部通过：init→声明 data→越权写 `# rogue edit`→verify **dirty 且点名 project.yaml**→stage_declare rogue **被拒**→restore→verify **clean**。
2. I3 复验：**越权窗口守卫已生效**（1.5 拒因即「受管区已被工程外部的改动覆盖」）。
3. 1.6 备注：template_declare rogue 确实被拒，但命中的是更早的 `脚本 probe 尚未声明` 校验，未走到越权守卫分支，故该项不能单独证明模板写入路径受守卫保护。
4. 2.1–2.6 全部通过：runId=`scratch5-exit3-20260908-111811-554`，job=`research.run-1`。
5. **D4 明确回答：detail 里的退出码是 3，不是 1**（run_query「退出码 3」、registry `"detail":"退出码 3"`、job 通知「退出码 3」三处一致）。
6. 对照 2.6：直接执行 `python scripts/exit3.py` → stdout `exit probe`，`$LASTEXITCODE=3`，与台账完全对齐 → 上轮 D4 缺陷判定**已修复**。
7. 收尾只读 verify 仍为 clean；除 1.3 授权的越权写入外，未直接改动任何受管文件。
8. 环境：Python 3.13.5；run 产出仅 config.json + manifest.json（raw 为空）。
